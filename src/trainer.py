import torch, torch.nn as nn
from torch.utils.data import DataLoader, random_split, TensorDataset

def train(model: nn.Module, X, y, train_ratio, lr, batch_size, epochs):
    # Siapkan DataLoader, optimizer, criterion, loop epoch…
    return history_dict
