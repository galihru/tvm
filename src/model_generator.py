def generate_pytorch_code(config: dict, dataset_name: str, data_type: str):
    # Konstruksi string kode berdasarkan config. …
    # Sama dengan metode generate_model_code() Anda,
    # tapi menerima `config` & `data_type` sebagai param.
    return code_str
